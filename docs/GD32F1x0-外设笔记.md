# GD32F1x0 外设笔记

> MCU：GD32F130（Cortex-M3）  
> 工程路径：`servo_project/`  
> 标准库：`servo_project/Library/`  
> 编写日期：2026-06-28

本文档记录 **GD32 片上外设的通用用法**（GPIO、RCU、Timer 等），与具体应用（电机、通信）无关。  
应用层笔记见：[M-01-电机驱动实现笔记.md](./M-01-电机驱动实现笔记.md)

---

## 文档结构

| 章节 | 外设 | 状态 |
|------|------|------|
| §1 | 库文件与头文件入口 | 已完成 |
| §2 | RCU 时钟 | 已完成 |
| §3 | GPIO | 已完成 |
| §4 | Timer（PWM） | 已完成 |
| §5 | I2C（AS5600） | 已完成 |
| §6 | USART | 已完成 |
| §7 | ADC | 已完成 |

---

## 1. 库文件与头文件入口

### 1.1 文件位置

| 层级 | 路径 | 作用 |
|------|------|------|
| 统一入口 | `User/gd32f1x0.h`（通过 Keil Pack） | 芯片寄存器定义 |
| 可选包含 | `User/gd32f1x0_libopt.h` | 按需 `#include` 各外设头文件 |
| 外设声明 | `Library/Include/gd32f1x0_*.h` | API 原型、宏、枚举 |
| 外设实现 | `Library/Source/gd32f1x0_*.c` | 库函数实现（一般不改） |

`gd32f1x0_libopt.h` 已包含 GPIO、RCU、Timer、I2C、USART、ADC 等头文件。  
用户代码 **只 `#include "gd32f1x0.h"`** 即可，不要单独 `#include "gd32f1x0_gpio.h"`。

> **clangd 提示：** `gd32f1x0_gpio.h` 内部会再包含 `gd32f1x0.h`，而 `gd32f1x0.h` 又通过 `gd32f1x0_libopt.h` 回头包含 `gd32f1x0_gpio.h`。在用户头文件里直接 include `gd32f1x0_gpio.h` 会触发 clangd 报 `main file cannot be included recursively`。Keil 编译通常仍能通过，但 IDE 会误报。

### 1.2 代码分层建议

```text
servo_project/User/
├── board_config.h          # 引脚宏（只定义，不写逻辑）
├── bsp/
│   ├── bsp_gpio.c/h        # GPIO 封装
│   ├── bsp_pwm.c/h         # Timer PWM 封装
│   ├── bsp_i2c.c/h         # I2C 封装（待建）
│   └── ...
├── servo/                  # 应用层（motor、pid 等）
└── main.c                  # 只调 bsp / 应用接口
```

**原则：** `main.c` 不直接操作寄存器；底层调用 `Library` 里的标准库函数；引脚定义集中在 `board_config.h`。

---

## 2. RCU 时钟

### 2.1 相关文件

- 头文件：[`Library/Include/gd32f1x0_rcu.h`](../servo_project/Library/Include/gd32f1x0_rcu.h)
- 源文件：[`Library/Source/gd32f1x0_rcu.c`](../servo_project/Library/Source/gd32f1x0_rcu.c)

### 2.2 常用 API

```c
void rcu_periph_clock_enable(rcu_periph_enum periph);
void rcu_periph_clock_disable(rcu_periph_enum periph);
```

### 2.3 常用时钟宏

| 宏 | 含义 |
|----|------|
| `RCU_GPIOA` / `RCU_GPIOB` / `RCU_GPIOF` | GPIO 端口时钟 |
| `RCU_TIMER2` | 定时器 2 |
| `RCU_I2C0` / `RCU_I2C1` | I2C0 / I2C1（本板 AS5600 用 **I2C0**） |
| `RCU_USART0` | USART0（APB2） |
| `RCU_USART1` | USART1（APB1） |
| `RCU_ADC` | ADC |

### 2.4 使用规则

**任何外设操作前，必须先使能对应时钟：**

```c
rcu_periph_clock_enable(RCU_GPIOA);   /* 操作 PA 口之前 */
rcu_periph_clock_enable(RCU_TIMER2); /* 配置 TIMER2 之前 */
```

---

## 3. GPIO

### 3.1 相关文件

| 文件 | 路径 |
|------|------|
| 头文件（查 API） | [`Library/Include/gd32f1x0_gpio.h`](../servo_project/Library/Include/gd32f1x0_gpio.h) |
| 源文件（实现） | [`Library/Source/gd32f1x0_gpio.c`](../servo_project/Library/Source/gd32f1x0_gpio.c) |

### 3.2 常用 API（`gd32f1x0_gpio.h` 约 365–390 行）

| 函数 | 作用 |
|------|------|
| `gpio_mode_set(port, mode, pull, pin)` | 设输入 / 输出 / 复用 / 模拟 |
| `gpio_output_options_set(port, otype, speed, pin)` | 推挽/开漏、输出速度 |
| `gpio_af_set(port, af, pin)` | 复用功能编号（PWM、I2C 等） |
| `gpio_bit_set(port, pin)` | 输出高 |
| `gpio_bit_reset(port, pin)` | 输出低 |
| `gpio_bit_write(port, pin, value)` | 写指定位 |
| `gpio_input_bit_get(port, pin)` | 读输入电平 |

### 3.3 常用宏

**模式（`gpio_mode_set` 第 2 参数）：**

| 宏 | 含义 |
|----|------|
| `GPIO_MODE_OUTPUT` | 普通输出 |
| `GPIO_MODE_INPUT` | 输入 |
| `GPIO_MODE_AF` | 复用（Timer、I2C、USART） |
| `GPIO_MODE_ANALOG` | 模拟（ADC） |

**上下拉（第 3 参数）：**

| 宏 | 含义 |
|----|------|
| `GPIO_PUPD_NONE` | 浮空 |
| `GPIO_PUPD_PULLUP` | 上拉 |
| `GPIO_PUPD_PULLDOWN` | 下拉 |

**输出类型与速度（`gpio_output_options_set`）：**

| 宏 | 含义 |
|----|------|
| `GPIO_OTYPE_PP` | 推挽 |
| `GPIO_OTYPE_OD` | 开漏 |
| `GPIO_OSPEED_2MHZ` | 2 MHz（#SD、LED 等） |
| `GPIO_OSPEED_50MHZ` | 50 MHz（PWM 等） |

**端口与引脚：**

```c
GPIOA, GPIOB, GPIOF          /* 端口 */
GPIO_PIN_0 ~ GPIO_PIN_15      /* 位掩码，可 OR 组合 */
GPIO_AF_0 ~ GPIO_AF_11       /* 复用编号，查数据手册 AF 表 */
```

### 3.4 普通 GPIO 输出 — 标准四步

```c
rcu_periph_clock_enable(RCU_GPIOx);                                    /* ① 开时钟 */
gpio_mode_set(GPIOx, GPIO_MODE_OUTPUT, GPIO_PUPD_NONE, GPIO_PIN_n);   /* ② 模式 */
gpio_output_options_set(GPIOx, GPIO_OTYPE_PP, GPIO_OSPEED_2MHZ, GPIO_PIN_n); /* ③ 输出属性 */
gpio_bit_set(GPIOx, GPIO_PIN_n);   /* 或 gpio_bit_reset — ④ 电平 */
```

### 3.5 普通 GPIO 输入

```c
rcu_periph_clock_enable(RCU_GPIOx);
gpio_mode_set(GPIOx, GPIO_MODE_INPUT, GPIO_PUPD_PULLUP, GPIO_PIN_n);

if (gpio_input_bit_get(GPIOx, GPIO_PIN_n) == SET) {
    /* 高电平 */
}
```

### 3.6 复用输出（AF，如 PWM / I2C）

与普通 GPIO 不同，需要额外一步：

```c
rcu_periph_clock_enable(RCU_GPIOA);
gpio_af_set(GPIOA, GPIO_AF_0, GPIO_PIN_7);                           /* ① AF 编号 */
gpio_mode_set(GPIOA, GPIO_MODE_AF, GPIO_PUPD_NONE, GPIO_PIN_7);      /* ② 复用模式 */
gpio_output_options_set(GPIOA, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, GPIO_PIN_7);
/* ③ 还需配置对应 Timer / I2C / USART 外设 */
```

### 3.7 本板 GPIO 引脚速查

| 引脚 | 功能 | GPIO 模式 |
|------|------|-----------|
| PF0 | LED1 | `GPIO_MODE_OUTPUT` 推挽 |
| PA6 | EG2104 #SD | `GPIO_MODE_OUTPUT` 推挽，低关断 |
| PA7 | M+ PWM | `GPIO_MODE_AF` + TIMER2_CH1 |
| PB1 | M- PWM | `GPIO_MODE_AF` + TIMER2_CH3 |
| PA9/PA10 | AS5600 I2C | `GPIO_MODE_AF` + AF4（**I2C0**） |
| PA2 | 上位机单线串口 | `GPIO_MODE_AF` + AF1（**USART1_TX**，半双工） |
| PA3/PA4 | ADC 采样 | `GPIO_MODE_ANALOG` |

引脚详情见 [硬件说明 §8](./硬件说明-16999-PS26040802.md)。

### 3.8 BSP 示例：LED 闪烁（PF0）

**`board_config.h`：**

```c
#define LED_PORT    GPIOF
#define LED_PIN     GPIO_PIN_0
```

**`bsp_gpio.c`：**

```c
#include "bsp_gpio.h"
#include "gd32f1x0_rcu.h"

void bsp_gpio_init(void)
{
    rcu_periph_clock_enable(RCU_GPIOF);
    gpio_mode_set(LED_PORT, GPIO_MODE_OUTPUT, GPIO_PUPD_NONE, LED_PIN);
    gpio_output_options_set(LED_PORT, GPIO_OTYPE_PP, GPIO_OSPEED_2MHZ, LED_PIN);
    gpio_bit_reset(LED_PORT, LED_PIN);
}

void bsp_led_on(void)  { gpio_bit_set(LED_PORT, LED_PIN); }
void bsp_led_off(void) { gpio_bit_reset(LED_PORT, LED_PIN); }
```

**`main.c`：**

```c
bsp_gpio_init();
while (1) {
    bsp_led_on();  delay_1ms(500);
    bsp_led_off(); delay_1ms(500);
}
```

### 3.9 Keil 集成注意

- 库文件 `gd32f1x0_gpio.c` 已在工程中（Library 组）
- 自写 `bsp_gpio.c` 需手动 **Add to Group → User**
- `board_config.h` 只参与编译包含，不必单独勾选编译

---

## 4. Timer（PWM）

PWM 由 **定时器时基**（PSC、ARR）+ **通道输出比较**（compare）共同决定。  
GPIO 复用部分见 [§3.6](#36-复用输出af如-pwm--i2c)；电机正反转、duty 范围、#SD 使能见 [M-01-电机驱动实现笔记.md §3–§5](./M-01-电机驱动实现笔记.md)。  
电流环同步采样（中心对齐 + TRGO 触发 ADC）见 [§4.10](#410-中心对齐-pwm--trgo-触发-adc电流环同步)。  
速度/位置环等 **控制环节拍**（定时器 UPDATE 中断）见 [§4.11](#411-定时器-update-中断控制环节拍)。

### 4.1 相关文件

| 文件 | 路径 |
|------|------|
| 头文件（查 API） | [`Library/Include/gd32f1x0_timer.h`](../servo_project/Library/Include/gd32f1x0_timer.h) |
| 源文件（实现） | [`Library/Source/gd32f1x0_timer.c`](../servo_project/Library/Source/gd32f1x0_timer.c) |

与 GPIO 相同，用户代码 **只 `#include "gd32f1x0.h"`**，不要单独 include `gd32f1x0_timer.h`。

### 4.2 频率与占空比

TIMER2 挂在 **APB1**。GD32 在 APB 预分频 ≠ 1 时，定时器时钟通常 **×2**；本工程 `SystemCoreClock = 72 MHz` 且 APB1 常为 ÷2 时，TIMER2 输入时钟一般为 **72 MHz**（以 `system_gd32f1x0.c` 实际配置为准）。

```
f_pwm = f_timer / ((PSC + 1) × (ARR + 1))
占空比 = compare / (ARR + 1)
```

本板目标 **20 kHz**，取 `PSC = 0`、`ARR = 3599`（分频系数 3600）：

```
72000000 / 3600 = 20000 Hz
```

compare 取值范围 `0 ~ ARR`（即 `0 ~ 3599`）。频率不对时用示波器微调 ARR/PSC。

### 4.3 常用 API

**时基：**

| 函数 | 作用 |
|------|------|
| `timer_deinit(timer)` | 复位定时器（初始化前可选调用） |
| `timer_struct_para_init(&para)` | 填充时基结构体默认值 |
| `timer_init(timer, &para)` | 配置 PSC、ARR、计数方向 |
| `timer_enable(timer)` | 启动计数 |
| `timer_disable(timer)` | 停止计数 |

**通道输出（PWM）：**

| 函数 | 作用 |
|------|------|
| `timer_channel_output_struct_para_init(&para)` | 填充通道结构体默认值 |
| `timer_channel_output_config(timer, ch, &para)` | 配置通道输出属性 |
| `timer_channel_output_mode_config(timer, ch, mode)` | 设为 PWM 模式（如 `TIMER_OC_MODE_PWM0`） |
| `timer_channel_output_pulse_value_config(timer, ch, pulse)` | 写 compare（占空比） |
| `timer_channel_output_shadow_config(timer, ch, shadow)` | 是否启用 compare 影子寄存器 |

**中断（控制环节拍，见 [§4.11](#411-定时器-update-中断控制环节拍)）：**

| 函数 | 作用 |
|------|------|
| `timer_interrupt_enable(timer, interrupt)` | 使能定时器中断（如 `TIMER_INT_UP`） |
| `timer_interrupt_disable(timer, interrupt)` | 关闭定时器中断 |
| `timer_interrupt_flag_get(timer, flag)` | 读中断标志（如 `TIMER_INT_FLAG_UP`） |
| `timer_interrupt_flag_clear(timer, flag)` | 清中断标志 |
| `nvic_irq_enable(irqn, pre, sub)` | 开 NVIC（见 §2 或 `gd32f1x0_misc.c`） |

### 4.4 常用宏

**定时器与通道：**

```c
TIMER0, TIMER1, TIMER2, TIMER13, ...   /* 定时器实例 */
TIMER_CH_0 ~ TIMER_CH_3               /* 通道编号 */
RCU_TIMER2                            /* TIMER2 时钟（见 §2.3） */
```

**时基（`timer_init` 的 `timer_parameter_struct`）：**

| 宏 | 含义 |
|----|------|
| `TIMER_COUNTER_EDGE` | 边沿对齐（普通 PWM 用这个） |
| `TIMER_COUNTER_UP` | 向上计数 |
| `TIMER_CKDIV_DIV1` | 时钟不分频 |

**通道输出（`timer_oc_parameter_struct` + 模式）：**

| 宏 | 含义 |
|----|------|
| `TIMER_CCX_ENABLE` | 使能通道输出 |
| `TIMER_CCXN_DISABLE` | 禁用互补输出 |
| `TIMER_OC_POLARITY_HIGH` | 有效电平为高 |
| `TIMER_OC_MODE_PWM0` | PWM 模式 0（计数 < compare 为高） |
| `TIMER_OC_SHADOW_DISABLE` | compare 立即生效 |

**中断（`timer_interrupt_enable` 第 2 参数 / `timer_interrupt_flag_get` 第 2 参数）：**

| 宏 | 含义 |
|----|------|
| `TIMER_INT_UP` | 更新（溢出）中断使能 |
| `TIMER_INT_FLAG_UP` | 更新中断标志 |
| `TIMER13_IRQn` | TIMER13 在 NVIC 中的编号（`gd32f1x0.h`） |
| `RCU_TIMER13` | TIMER13 时钟 |

> **说明：** `TIMER_OC_MODE_PWM0` 下，compare = 0 时输出全程低；compare = ARR 时接近 100% 高。  
> TIMER2 为通用定时器，**不需要** `timer_primary_output_config()` 等高级定时器刹车/互补配置。

### 4.5 本板 Timer 复用速查

查 GD32F130 数据手册 §2.6.7：

| 引脚 | 功能 | Timer 通道 | AF |
|------|------|------------|-----|
| PA7 | M+ PWM | TIMER2 **CH1** | AF0 |
| PB1 | M- PWM | TIMER2 **CH3** | AF0 |

两路 PWM **共用 TIMER2**，compare 值可直接对比。

> **例程陷阱：** `gpio_output` 例程把 PA6 当第二路 PWM、PB1 当 #SD，与原理图相反。必须以 [硬件说明 §8.3](./硬件说明-16999-PS26040802.md) 为准：**PA7=CH1，PB1=CH3，PA6=#SD**。

### 4.6 PWM 输出 — 标准六步

与 GPIO「开时钟 → 设模式 → 写电平」对应：

```text
① 开时钟（GPIO + TIMER）
② GPIO 复用（AF → MODE_AF → 50 MHz 推挽，见 §3.6）
③ 定时器时基（PSC、ARR、向上计数）
④ 通道输出（PWM0 模式、极性、使能）
⑤ compare = 0（上电无输出）
⑥ timer_enable() 启动
```

```c
rcu_periph_clock_enable(RCU_GPIOA);
rcu_periph_clock_enable(RCU_GPIOB);
rcu_periph_clock_enable(RCU_TIMER2);                              /* ① */

gpio_af_set(GPIOA, GPIO_AF_0, GPIO_PIN_7);                        /* ② */
gpio_af_set(GPIOB, GPIO_AF_0, GPIO_PIN_1);
gpio_mode_set(GPIOA, GPIO_MODE_AF, GPIO_PUPD_NONE, GPIO_PIN_7);
gpio_mode_set(GPIOB, GPIO_MODE_AF, GPIO_PUPD_NONE, GPIO_PIN_1);
gpio_output_options_set(GPIOA, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, GPIO_PIN_7);
gpio_output_options_set(GPIOB, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, GPIO_PIN_1);

timer_parameter_struct timer_initpara;
timer_oc_parameter_struct timer_ocinitpara;

timer_deinit(TIMER2);                                             /* ③ */
timer_struct_para_init(&timer_initpara);
timer_initpara.prescaler         = 0U;
timer_initpara.alignedmode       = TIMER_COUNTER_EDGE;
timer_initpara.counterdirection  = TIMER_COUNTER_UP;
timer_initpara.period            = 3599U;   /* ARR */
timer_initpara.clockdivision     = TIMER_CKDIV_DIV1;
timer_init(TIMER2, &timer_initpara);

timer_channel_output_struct_para_init(&timer_ocinitpara);        /* ④ */
timer_ocinitpara.outputstate  = TIMER_CCX_ENABLE;
timer_ocinitpara.outputnstate = TIMER_CCXN_DISABLE;
timer_ocinitpara.ocpolarity   = TIMER_OC_POLARITY_HIGH;
timer_ocinitpara.ocnpolarity  = TIMER_OCN_POLARITY_HIGH;
timer_ocinitpara.ocidlestate  = TIMER_OC_IDLE_STATE_LOW;
timer_ocinitpara.ocnidlestate = TIMER_OCN_IDLE_STATE_LOW;

timer_channel_output_config(TIMER2, TIMER_CH_1, &timer_ocinitpara);
timer_channel_output_config(TIMER2, TIMER_CH_3, &timer_ocinitpara);
timer_channel_output_mode_config(TIMER2, TIMER_CH_1, TIMER_OC_MODE_PWM0);
timer_channel_output_mode_config(TIMER2, TIMER_CH_3, TIMER_OC_MODE_PWM0);
timer_channel_output_shadow_config(TIMER2, TIMER_CH_1, TIMER_OC_SHADOW_DISABLE);
timer_channel_output_shadow_config(TIMER2, TIMER_CH_3, TIMER_OC_SHADOW_DISABLE);

timer_channel_output_pulse_value_config(TIMER2, TIMER_CH_1, 0U);  /* ⑤ */
timer_channel_output_pulse_value_config(TIMER2, TIMER_CH_3, 0U);

timer_enable(TIMER2);                                             /* ⑥ */
```

运行时改占空比只需调用 `timer_channel_output_pulse_value_config()`，不必重启定时器。

### 4.7 BSP 示例：`bsp_pwm.c/h`

**`board_config.h`：**

```c
#define MOTOR_PWM_FWD_PORT        GPIOA
#define MOTOR_PWM_FWD_PIN         GPIO_PIN_7
#define MOTOR_PWM_FWD_CH          TIMER_CH_1

#define MOTOR_PWM_REV_PORT        GPIOB
#define MOTOR_PWM_REV_PIN         GPIO_PIN_1
#define MOTOR_PWM_REV_CH          TIMER_CH_3

#define MOTOR_PWM_TIMER           TIMER2
#define MOTOR_PWM_TIMER_RCU       RCU_TIMER2
#define MOTOR_PWM_TIMER_AF        GPIO_AF_0

#define MOTOR_PWM_FREQ_HZ         20000U
#define MOTOR_PWM_PERIOD          3599U   /* ARR，20 kHz @ 72 MHz TIMER 时钟 */
```

**`bsp_pwm.h`：**

```c
#ifndef BSP_PWM_H
#define BSP_PWM_H
#include "gd32f1x0.h"

void bsp_pwm_init(void);
void bsp_pwm_set_duty(int16_t duty);   /* ±MOTOR_PWM_PERIOD，正负表方向 */

#endif
```

**`bsp_pwm.c`（核心逻辑）：**

```c
#include "bsp_pwm.h"
#include "board_config.h"

static void pwm_set_compare(uint16_t channel, uint16_t pulse)
{
    if (pulse > MOTOR_PWM_PERIOD) {
        pulse = MOTOR_PWM_PERIOD;
    }
    timer_channel_output_pulse_value_config(MOTOR_PWM_TIMER, channel, pulse);
}

void bsp_pwm_init(void)
{
    timer_parameter_struct timer_initpara;
    timer_oc_parameter_struct timer_ocinitpara;

    rcu_periph_clock_enable(RCU_GPIOA);
    rcu_periph_clock_enable(RCU_GPIOB);
    rcu_periph_clock_enable(MOTOR_PWM_TIMER_RCU);

    gpio_af_set(MOTOR_PWM_FWD_PORT, MOTOR_PWM_TIMER_AF, MOTOR_PWM_FWD_PIN);
    gpio_af_set(MOTOR_PWM_REV_PORT, MOTOR_PWM_TIMER_AF, MOTOR_PWM_REV_PIN);
    gpio_mode_set(MOTOR_PWM_FWD_PORT, GPIO_MODE_AF, GPIO_PUPD_NONE, MOTOR_PWM_FWD_PIN);
    gpio_mode_set(MOTOR_PWM_REV_PORT, GPIO_MODE_AF, GPIO_PUPD_NONE, MOTOR_PWM_REV_PIN);
    gpio_output_options_set(MOTOR_PWM_FWD_PORT, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, MOTOR_PWM_FWD_PIN);
    gpio_output_options_set(MOTOR_PWM_REV_PORT, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, MOTOR_PWM_REV_PIN);

    timer_deinit(MOTOR_PWM_TIMER);
    timer_struct_para_init(&timer_initpara);
    timer_initpara.prescaler         = 0U;
    timer_initpara.alignedmode       = TIMER_COUNTER_EDGE;
    timer_initpara.counterdirection  = TIMER_COUNTER_UP;
    timer_initpara.period            = MOTOR_PWM_PERIOD;
    timer_initpara.clockdivision     = TIMER_CKDIV_DIV1;
    timer_init(MOTOR_PWM_TIMER, &timer_initpara);

    timer_channel_output_struct_para_init(&timer_ocinitpara);
    timer_ocinitpara.outputstate  = TIMER_CCX_ENABLE;
    timer_ocinitpara.outputnstate = TIMER_CCXN_DISABLE;
    timer_ocinitpara.ocpolarity   = TIMER_OC_POLARITY_HIGH;
    timer_ocinitpara.ocnpolarity  = TIMER_OCN_POLARITY_HIGH;
    timer_ocinitpara.ocidlestate  = TIMER_OC_IDLE_STATE_LOW;
    timer_ocinitpara.ocnidlestate = TIMER_OCN_IDLE_STATE_LOW;

    timer_channel_output_config(MOTOR_PWM_TIMER, MOTOR_PWM_FWD_CH, &timer_ocinitpara);
    timer_channel_output_config(MOTOR_PWM_TIMER, MOTOR_PWM_REV_CH, &timer_ocinitpara);
    timer_channel_output_mode_config(MOTOR_PWM_TIMER, MOTOR_PWM_FWD_CH, TIMER_OC_MODE_PWM0);
    timer_channel_output_mode_config(MOTOR_PWM_TIMER, MOTOR_PWM_REV_CH, TIMER_OC_MODE_PWM0);
    timer_channel_output_shadow_config(MOTOR_PWM_TIMER, MOTOR_PWM_FWD_CH, TIMER_OC_SHADOW_DISABLE);
    timer_channel_output_shadow_config(MOTOR_PWM_TIMER, MOTOR_PWM_REV_CH, TIMER_OC_SHADOW_DISABLE);

    pwm_set_compare(MOTOR_PWM_FWD_CH, 0U);
    pwm_set_compare(MOTOR_PWM_REV_CH, 0U);
    timer_enable(MOTOR_PWM_TIMER);
}

void bsp_pwm_set_duty(int16_t duty)
{
    uint16_t mag;

    if (duty > (int16_t)MOTOR_PWM_PERIOD) {
        duty = (int16_t)MOTOR_PWM_PERIOD;
    } else if (duty < -(int16_t)MOTOR_PWM_PERIOD) {
        duty = -(int16_t)MOTOR_PWM_PERIOD;
    }

    if (duty >= 0) {
        mag = (uint16_t)duty;
        pwm_set_compare(MOTOR_PWM_FWD_CH, mag);
        pwm_set_compare(MOTOR_PWM_REV_CH, 0U);
    } else {
        mag = (uint16_t)(-duty);
        pwm_set_compare(MOTOR_PWM_FWD_CH, 0U);
        pwm_set_compare(MOTOR_PWM_REV_CH, mag);
    }
}
```

`bsp_pwm_set_duty()` 的互斥逻辑（同一时刻只给一侧 IN 加 PWM）属于电机应用策略，详见 [M-01 §3.2](./M-01-电机驱动实现笔记.md#32-正反转策略互补单路-pwm)。  
#SD（PA6）为普通 GPIO，不在 `bsp_pwm` 里配置，由 `motor.c` 或单独 BSP 管理。

**`main.c` 最小调用：**

```c
#include "bsp_pwm.h"
#include "systick.h"

int main(void)
{
    systick_config();
    bsp_pwm_init();

    while (1) {
        bsp_pwm_set_duty(1200);   /* 正转 */
        delay_1ms(2000);
        bsp_pwm_set_duty(0);      /* 停止 */
        delay_1ms(500);
        bsp_pwm_set_duty(-1200);  /* 反转 */
        delay_1ms(2000);
        bsp_pwm_set_duty(0);
        delay_1ms(500);
    }
}
```

### 4.8 Keil 集成注意

- 库文件 `gd32f1x0_timer.c` 已在工程中（Library 组）
- 自写 `bsp_pwm.c` 需手动 **Add to Group → User**
- `board_config.h` 只参与编译包含，不必单独勾选编译

### 4.9 调试速查

| 现象 | 排查 |
|------|------|
| 无波形 | 是否 `timer_enable()`；PA7/PB1 是否 AF0；通道是否为 **CH1/CH3**（不是例程的 CH0/CH1） |
| 只一侧有波 | 检查 PB1 的 AF0 + TIMER2_CH3 配置 |
| 频率不对 | 查 `SystemCoreClock`，重算 ARR/PSC |
| 电机发热 | 两路 compare 是否互斥；#SD 默认是否低 |

### 4.10 中心对齐 PWM + TRGO 触发 ADC（电流环同步）

开环阶段 §4.6 的 **边沿对齐** 足够。做 **电流闭环** 时，建议改为 **中心对齐 PWM**，并由 **TIMER2 TRGO** 触发 ADC 采样（ADC 侧配置见 [§7.12](#712-timer-触发--dma--中断完整链路)）。

#### 4.10.1 信号链路

```text
TIMER2（中心对齐 PWM，持续运行）
    │ TRGO（Update 事件）
    ▼
ADC 规则组扫描（CH3 电流 + CH4 电压）
    │ 转换完成
    ├── DMA ──► 内存缓冲 [I_raw, V_raw, ...]
    └── ADC EOC 中断 ──► 读缓冲 / 电流环 ──► 写 TIMER2 CCR（下一周期生效）
```

**要点：**

- **PWM 触发 ADC**，不是 ADC 触发 PWM 时基；TIMER 自由运行，ISR 只更新 **CCR**。
- 采样时刻应落在 PWM **谷底/顶点**（电流较稳），中心对齐比边沿对齐更合适。
- CCR 建议开 **影子寄存器**，在 UPDATE 事件加载，避免 duty 毛刺。

#### 4.10.2 中心对齐频率公式

```
f_pwm = f_timer / (2 × (PSC + 1) × (ARR + 1))    /* 中心对齐 */
占空比 = compare / ARR                            /* 中心对齐时 compare 相对 ARR */
```

目标 **20 kHz**、TIMER 时钟 **72 MHz**、PSC = 0：

```
ARR = 72_000_000 / (2 × 20_000) - 1 = 1799
```

compare 范围 **0 ~ ARR**（0 ~ 1799）。与边沿对齐 §4.2 的 ARR = 3599 **不可混用**。

#### 4.10.3 额外 API（主模式 / 影子）

| 函数 | 作用 |
|------|------|
| `timer_master_output_trigger_source_select(timer, src)` | 选择 **TRGO** 输出源 |
| `timer_auto_reload_shadow_enable(timer)` | ARR 影子寄存器 |
| `timer_channel_output_shadow_config(timer, ch, shadow)` | CCR 影子寄存器 |

#### 4.10.4 额外宏

**中心对齐（`timer_init` → `alignedmode`）：**

| 宏 | 含义 |
|----|------|
| `TIMER_COUNTER_EDGE` | 边沿对齐（§4.6 开环用这个） |
| `TIMER_COUNTER_CENTER_DOWN` | 中心对齐，向下计数比较有效（**常用**） |
| `TIMER_COUNTER_CENTER_UP` | 中心对齐，向上计数比较有效 |
| `TIMER_COUNTER_CENTER_BOTH` | 中心对齐，上下均有效 |

**TRGO 源（`timer_master_output_trigger_source_select` 第 2 参数）：**

| 宏 | 含义 |
|----|------|
| `TIMER_TRI_OUT_SRC_UPDATE` | **更新事件**作 TRGO（与 ADC 同步最常用） |
| `TIMER_TRI_OUT_SRC_ENABLE` | CEN 使能信号 |
| `TIMER_TRI_OUT_SRC_CH0` ~ `O3CPRE` | 通道比较事件 |

本板选 **`TIMER_TRI_OUT_SRC_UPDATE`**：计数器回零/换向时触发 ADC，对应 PWM 周期锚点。

#### 4.10.5 TIMER2 侧 — 配置步骤

在 §4.6 六步基础上 **改动以下项**：

```text
① 时基改为 center-aligned，ARR 按 §4.10.2 重算（1799 @ 20 kHz）
② timer_master_output_trigger_source_select(TIMER2, TIMER_TRI_OUT_SRC_UPDATE)
③ timer_auto_reload_shadow_enable(TIMER2)
④ 两路 CCR 开影子：timer_channel_output_shadow_config(..., TIMER_OC_SHADOW_ENABLE)
⑤ PWM 通道配置不变（CH1/CH3、PWM0）
⑥ timer_enable() 后 TIMER2 即按 TRGO 周期触发 ADC（ADC 侧须已配外部触发）
```

```c
timer_parameter_struct timer_initpara;
timer_oc_parameter_struct timer_ocinitpara;

timer_deinit(TIMER2);
timer_struct_para_init(&timer_initpara);
timer_initpara.prescaler         = 0U;
timer_initpara.alignedmode       = TIMER_COUNTER_CENTER_DOWN;  /* 中心对齐 */
timer_initpara.counterdirection  = TIMER_COUNTER_UP;
timer_initpara.period            = 1799U;                      /* 20 kHz @ 72 MHz */
timer_initpara.clockdivision     = TIMER_CKDIV_DIV1;
timer_init(TIMER2, &timer_initpara);

/* ... CH1/CH3 PWM 配置同 §4.6 ... */

timer_master_output_trigger_source_select(TIMER2, TIMER_TRI_OUT_SRC_UPDATE);
timer_auto_reload_shadow_enable(TIMER2);
timer_channel_output_shadow_config(TIMER2, TIMER_CH_1, TIMER_OC_SHADOW_ENABLE);
timer_channel_output_shadow_config(TIMER2, TIMER_CH_3, TIMER_OC_SHADOW_ENABLE);

timer_enable(TIMER2);
```

**ISR 中更新 duty：** 只调 `timer_channel_output_pulse_value_config()` 写 CCR；影子寄存器会在下一次 UPDATE 生效。

#### 4.10.6 调试速查（PWM + ADC 同步）

| 现象 | 排查 |
|------|------|
| ADC 不采样 | ADC 是否 `adc_external_trigger_config(ENABLE)` 且源为 `T2_TRGO`；TIMER2 是否已 enable |
| 采样频率不对 | 中心对齐 ARR 是否按 §4.10.2 重算；TRGO 是否为 UPDATE |
| duty 更新有毛刺 | CCR 是否开影子；ISR 中勿 `timer_enable/disable` |
| 电流采样噪声大 | TRGO 是否在开关边沿；可试 `CENTER_DOWN` 或示波器对齐 TRGO 与 PWM |

### 4.11 定时器 UPDATE 中断（控制环节拍）

本工程 **速度环 / 位置环 / 电流轮询** 需要固定周期任务（如 1 kHz），用 **通用定时器的更新（UPDATE）中断** 实现，**不要** 在已用于 PWM 的 TIMER2 上再开 UPDATE 中断（20 kHz 会每周期进 ISR，负担过大且与 PWM 耦合）。

#### 4.11.1 本板定时器分工

| 定时器 | 用途 | 频率 | 中断 |
|--------|------|------|------|
| **TIMER2** | PA7/PB1 电机 PWM（§4.6） | 20 kHz | **不开** |
| **TIMER13** | 控制环节拍（`time.c`） | 1 kHz（可调） | UPDATE |
| **SysTick** | `delay_1ms()` 等延时 | 1 ms | 仅递减延时计数 |

工程参考：`servo_project/User/servo/time.c`、`time.h`。

#### 4.11.2 中断频率公式

与 §4.2 相同（边沿对齐、向上计数）：

```
f_irq = f_timer / ((PSC + 1) × (ARR + 1))
```

TIMER13 挂在 **APB1**，时钟规则同 TIMER2（本板通常 **72 MHz**）。

目标 **1 kHz** 常用分频：

```
PSC = 71,  ARR = 999  →  72_000_000 / 72 / 1000 = 1000 Hz
```

| 目标频率 | PSC | ARR |
|----------|-----|-----|
| 1 kHz（速度环起点） | 71 | 999 |
| 500 Hz | 71 | 1999 |
| 2 kHz | 71 | 499 |

#### 4.11.3 配置四步

```text
① 开 TIMER 时钟，配 PSC/ARR（仅时基，不配 PWM 通道）
② timer_interrupt_flag_clear + timer_interrupt_enable(TIMER_INT_UP)
③ nvic_irq_enable(TIMER13_IRQn, ...)
④ timer_enable() → 每个周期进一次 UPDATE 中断
```

```c
/* time.h */
#define CONTROL_TIMER       TIMER13
#define CONTROL_TIMER_RCU   RCU_TIMER13
#define CONTROL_TIMER_IRQn  TIMER13_IRQn
#define CONTROL_LOOP_HZ     1000U

void time_init(void);

/* time.c */
void time_init(void)
{
    timer_parameter_struct timer_initpara;

    rcu_periph_clock_enable(CONTROL_TIMER_RCU);
    timer_deinit(CONTROL_TIMER);
    timer_struct_para_init(&timer_initpara);
    timer_initpara.prescaler         = 71U;
    timer_initpara.alignedmode       = TIMER_COUNTER_EDGE;
    timer_initpara.counterdirection  = TIMER_COUNTER_UP;
    timer_initpara.period            = 999U;
    timer_initpara.clockdivision     = TIMER_CKDIV_DIV1;
    timer_initpara.repetitioncounter = 0U;
    timer_init(CONTROL_TIMER, &timer_initpara);

    timer_interrupt_flag_clear(CONTROL_TIMER, TIMER_INT_FLAG_UP);
    timer_interrupt_enable(CONTROL_TIMER, TIMER_INT_UP);
    nvic_irq_enable(CONTROL_TIMER_IRQn, 1U, 0U);

    timer_enable(CONTROL_TIMER);
}
```

#### 4.11.4 中断服务函数

函数名必须与启动文件向量表一致：**TIMER13 → `TIMER13_IRQHandler`**（见 `Startup/startup_gd32f1x0.s`）。

```c
void TIMER13_IRQHandler(void)
{
    if (RESET != timer_interrupt_flag_get(CONTROL_TIMER, TIMER_INT_FLAG_UP)) {
        timer_interrupt_flag_clear(CONTROL_TIMER, TIMER_INT_FLAG_UP);

        /* 本周期控制任务，保持简短 */
        electricity_update();      /* 电流/电压轮询 */
        /* speed_loop_update(); */   /* 速度环（后续） */
        /* position_loop_update(); */ /* 位置环降频或同 ISR */
    }
}
```

**ISR 约束：**

- 只做控制计算 + `motor_set_duty()` / 读 ADC；**禁止** `delay_1ms`、I2C 读 AS5600、`printf`
- 位置环可每 N 次中断执行一次（`tick % 10 == 0` → 100 Hz）
- NVIC 优先级：控制定时器建议 **高于** 非实时外设，低于 HardFault；本示例 `pre=1`

#### 4.11.5 初始化顺序

```text
systick_config()     /* 延时用 */
gpio_init()
adc_init()
motor_init()         /* 内含 pwm_init → TIMER2 启动 PWM */
time_init()          /* TIMER13 开始 1 kHz 中断 */
```

`main` 中 `while(1)` 可空转或跑慢任务（通信、标定）；**控制环在 ISR 里跑**。

#### 4.11.6 与 SysTick 的区别

| 项目 | SysTick | TIMER13 UPDATE |
|------|---------|----------------|
| 库默认用途 | `delay_1ms()` | 用户控制环 |
| 是否改周期 | 改会影响全局延时 | 独立，可随意设 500 Hz~2 kHz |
| 是否推荐做速度环 | 不推荐（与延时共用） | **推荐** |

#### 4.11.7 调试速查

| 现象 | 排查 |
|------|------|
| 不进中断 | 是否 `timer_interrupt_enable(TIMER_INT_UP)`；`nvic_irq_enable(TIMER13_IRQn)`；是否 `timer_enable` |
| 频率不对 | 重算 PSC/ARR；查 `SystemCoreClock` 是否为 72 MHz |
| 进一次就停 | 是否忘了 `timer_interrupt_flag_clear` |
| 电机抖动 | ISR 太长；浮点运算过多；与 20 kHz PWM 无关时应查 TIMER13 而非 TIMER2 |
| 链接错误 `TIMER13_IRQHandler` | `time.c` 是否加入 Keil User 组 |

可选替代：**TIMER14**（`TIMER14_IRQn`），用法相同，避免与其它外设冲突即可。

---

## 5. I2C（AS5600 位置传感器）

本板 I2C 用于读取 **AS5600-ASOM** 磁编码器角度。GPIO 复用见 [§3.6 / §3.7](#37-本板-gpio-引脚速查)；unwrap、速度估算、无磁铁保护见 [M-02-M03-感知采集实现笔记 §5](./M-02-M03-感知采集实现笔记.md)。

> **与 PWM 的区别：** I2C 引脚必须 **开漏（OD）+ 上拉**，不能配成推挽。  
> **与例程的差异：** 本板 **DIR 已在硬件接地**，**不要** 把 PA4 当 AS5600 DIR 输出（PA4 是 VIN ADC，见 [硬件说明 §7](./硬件说明-16999-PS26040802.md)）。

### 5.1 相关文件

| 文件 | 路径 |
|------|------|
| 头文件（查 API） | [`Library/Include/gd32f1x0_i2c.h`](../servo_project/Library/Include/gd32f1x0_i2c.h) |
| 源文件（实现） | [`Library/Source/gd32f1x0_i2c.c`](../servo_project/Library/Source/gd32f1x0_i2c.c) |
| BSP（本工程） | `User/bsp/bsp_i2c.c/h`（Keil 中暂名 `bap_i2c.c`，建议改名） |
| 应用驱动 | `User/servo/as5600.c/h`（待建） |
| AS5600 规格书 | `C79815_位置传感器_AS5600-ASOM_规格书_WJ105942.PDF` |

用户代码 **只 `#include "gd32f1x0.h"`**，不要单独 include `gd32f1x0_i2c.h`。

### 5.2 基本参数

| 项目 | 本板取值 |
|------|----------|
| 外设实例 | **I2C0**（数据手册 Pin17/18：PA9=I2C0_SCL，PA10=I2C0_SDA） |
| SCL / SDA | **PA9 / PA10**，复用 **AF4** |
| 总线速率 | **400 kHz**（Fast-mode；AS5600 最高 1 MHz） |
| 从机 7-bit 地址 | **0x36**（规格书固定，不可改） |
| I2C 时钟源 | **APB1**（与 TIMER2/13 同域；`SystemCoreClock = 72 MHz` 时 APB1 通常 36 MHz，`i2c_clock_config()` 内部会读 `rcu_clock_freq_get(CK_APB1)`） |
| 上拉电阻 | 板载 **R14/R15 = 1.5 kΩ** 至 3V3 |

**AS5600 默认无需写配置寄存器：** 上电 + 磁铁就位后，直接 I2C 读 **STATUS / ANGLE** 即可。CONF 等 OTP 烧录仅在改量程/输出模式时需要（本板 PGO 接 3V3，Programming Option B）。

### 5.3 AS5600 寄存器速查（规格书）

| 寄存器 | 字地址 | 宽度 | 说明 |
|--------|--------|------|------|
| STATUS | **0x0B** | 1 B | bit5 **MD** = 1：检测到磁铁；bit4 ML / bit3 MH：磁强过弱/过强 |
| RAW ANGLE | 0x0C | 2 B | 未滤波原始角，12-bit 有效 |
| **ANGLE** | **0x0E** | 2 B | **滤波后角度（常用）**，高字节在前，取 `(hi<<8|lo) & 0x0FFF` → 0~4095 |
| AGC | 0x1A | 1 B | 自动增益，调试磁强用 |

**角度换算：**

```text
角度(°) = raw × 360 / 4096
```

本板 **DIR 接 GND** → 顺时针旋转时 raw **增大**。

### 5.4 常用 API

**初始化：**

| 函数 | 作用 |
|------|------|
| `i2c_deinit(i2c)` | 复位 I2C（初始化前调用） |
| `i2c_clock_config(i2c, speed, dutycyc)` | 设 SCL 频率（如 400000 + `I2C_DTCY_2`） |
| `i2c_mode_addr_config(i2c, mode, addformat, addr)` | I2C 模式 + 本机 7-bit 地址（主机时可填 0） |
| `i2c_ack_config(i2c, I2C_ACK_ENABLE)` | 接收时发 ACK |
| `i2c_enable(i2c)` | 使能外设 |

**主机传输（轮询）：**

| 函数 | 作用 |
|------|------|
| `i2c_start_on_bus(i2c)` | 发送 START（读寄存器时第二次为 **Repeated START**） |
| `i2c_stop_on_bus(i2c)` | 发送 STOP |
| `i2c_master_addressing(i2c, addr, dir)` | 发从机地址；`dir` = `I2C_TRANSMITTER` / `I2C_RECEIVER` |
| `i2c_data_transmit(i2c, byte)` | 发 1 字节 |
| `i2c_data_receive(i2c)` | 收 1 字节 |
| `i2c_flag_get(i2c, flag)` | 读状态标志 |
| `i2c_flag_clear(i2c, flag)` | 清标志（`ADDSEND` 需专用清除序列） |

### 5.5 常用宏

**外设与时钟：**

```c
I2C0, RCU_I2C0                    /* 本板 AS5600 总线 */
GPIO_AF_4                         /* PA9/PA10 的 I2C0 复用 */
400000U                           /* Fast-mode 400 kHz */
I2C_DTCY_2                        /* 快速模式占空比 Tlow/Thigh = 2 */
I2C_I2CMODE_ENABLE                /* 标准 I2C（非 SMBus） */
I2C_ADDFORMAT_7BITS                /* 7-bit 地址 */
```

**传输方向：**

```c
I2C_TRANSMITTER, I2C_RECEIVER
I2C_ACK_ENABLE, I2C_ACK_DISABLE
```

**轮询常用标志（`i2c_flag_get` 第 2 参数）：**

| 宏 | 含义 |
|----|------|
| `I2C_FLAG_I2CBSY` | 总线忙，发 START 前应等待为 **RESET** |
| `I2C_FLAG_SBSEND` | START 已发送 |
| `I2C_FLAG_ADDSEND` | 地址已发送/匹配，需 `i2c_flag_clear` |
| `I2C_FLAG_TBE` | 发送缓冲空，可写下一字节 |
| `I2C_FLAG_BTC` | 字节传输完成 |
| `I2C_FLAG_RBNE` | 接收缓冲非空，可读 |
| `I2C_FLAG_AERR` | 无 ACK（从机未响应） |

**从机地址传参约定（GD32 标准库）：**

```c
#define AS5600_I2C_ADDR_7BIT    0x36U
#define AS5600_I2C_ADDR_WRITE   (AS5600_I2C_ADDR_7BIT << 1)   /* 0x6C → TRANSMITTER */
#define AS5600_I2C_ADDR_READ    (AS5600_I2C_ADDR_7BIT << 1)   /* 0x6C → RECEIVER 时库会 OR bit0 */
```

`i2c_master_addressing()` 的第 2 参数传 **7-bit 地址左移 1 位** 的值（与 GigaDevice 官方 I2C 例程一致）。

### 5.6 本板 I2C 引脚速查

| 引脚 | 信号 | AF | GPIO 模式 |
|------|------|-----|-----------|
| PA9 | I2C0_SCL | AF4 | `GPIO_MODE_AF` + **开漏 OD** + 上拉 |
| PA10 | I2C0_SDA | AF4 | 同上 |

硬件连接见 [硬件说明 §7.2](./硬件说明-16999-PS26040802.md)。

### 5.7 I2C 主机初始化 — 标准五步

与 GPIO「开时钟 → 设模式」对应：

```text
① 开时钟（GPIOA + I2C0）
② PA9/PA10：AF4 → MODE_AF → 开漏 OD + 上拉（见 §5.6）
③ i2c_deinit → i2c_clock_config(400k) → i2c_mode_addr_config(7bit, addr=0)
④ i2c_ack_config(ENABLE) → i2c_enable
⑤ 用 bsp_i2c_read_reg 读 AS5600 0x0B 验收
```

```c
rcu_periph_clock_enable(RCU_GPIOA);
rcu_periph_clock_enable(RCU_I2C0);                                 /* ① */

gpio_af_set(GPIOA, GPIO_AF_4, GPIO_PIN_9 | GPIO_PIN_10);           /* ② */
gpio_mode_set(GPIOA, GPIO_MODE_AF, GPIO_PUPD_PULLUP, GPIO_PIN_9 | GPIO_PIN_10);
gpio_output_options_set(GPIOA, GPIO_OTYPE_OD, GPIO_OSPEED_50MHZ, GPIO_PIN_9 | GPIO_PIN_10);

i2c_deinit(I2C0);                                                   /* ③ */
i2c_clock_config(I2C0, 400000U, I2C_DTCY_2);
i2c_mode_addr_config(I2C0, I2C_I2CMODE_ENABLE, I2C_ADDFORMAT_7BITS, 0x00U);
i2c_ack_config(I2C0, I2C_ACK_ENABLE);                              /* ④ */
i2c_enable(I2C0);
```

> **为何用开漏：** I2C 是线与总线，多主机/从机共享 SCL/SDA，只能 OD + 外部上拉。

### 5.8 读从机寄存器 — 轮询时序（读 ANGLE 0x0E）

AS5600 为 **「写寄存器指针 → Repeated START → 读数据」** 标准 I2C 读法：

```text
  START ──► [0x6C W] ──► [0x0E] ──► Re-START ──► [0x6D R] ──► [MSB] ACK ──► [LSB] NACK ──► STOP
```

**单字节读流程（以 STATUS 0x0B 为例）：**

```text
① 等 I2C_FLAG_I2CBSY == RESET
② START → 等 SBSEND
③ 发从机写地址 → 等 ADDSEND → clear ADDSEND
④ 发寄存器地址 reg → 等 BTC
⑤ Re-START → 等 SBSEND
⑥ 发从机读地址 → 等 ADDSEND → clear ADDSEND
⑦ 最后一字节前 i2c_ack_config(DISABLE)
⑧ 等 RBNE → i2c_data_receive → STOP → 恢复 ACK_ENABLE
```

**读 ANGLE（2 字节）要点：** 读第 1 字节后 **保持 ACK**；读第 2 字节前 **关闭 ACK**，读完后 STOP。

每步建议加 **超时**（如 10 ms，`systick` 递减），避免 SDA 被拉死时主循环卡死。收到 `I2C_FLAG_AERR` 时清标志并返回失败。

### 5.9 BSP 示例：`bsp_i2c.c/h`

**`board_config.h`（或集中宏头文件）：**

```c
#define I2C_PERIPH              I2C0
#define I2C_PERIPH_RCU          RCU_I2C0
#define I2C_GPIO_PORT           GPIOA
#define I2C_GPIO_AF             GPIO_AF_4
#define I2C_SCL_PIN             GPIO_PIN_9
#define I2C_SDA_PIN             GPIO_PIN_10
#define I2C_SPEED_HZ            400000U

#define AS5600_I2C_ADDR_7BIT    0x36U
#define AS5600_REG_STATUS       0x0BU
#define AS5600_REG_ANGLE        0x0EU
#define AS5600_STATUS_MD        0x20U   /* bit5：磁铁有效 */
```

**`bsp_i2c.h`：**

```c
#ifndef BSP_I2C_H
#define BSP_I2C_H
#include "gd32f1x0.h"
#include <stdbool.h>
#include <stdint.h>

void bsp_i2c_init(void);
bool bsp_i2c_read_reg(uint8_t dev_addr_7bit, uint8_t reg, uint8_t *data, uint8_t len);

#endif
```

**`bsp_i2c.c`（核心逻辑）：**

```c
#include "bsp_i2c.h"
#include "board_config.h"
#include "systick.h"

#define I2C_TIMEOUT_MS    10U

static bool i2c_wait_flag(i2c_flag_enum flag, uint32_t timeout_ms)
{
    while (RESET == i2c_flag_get(I2C_PERIPH, flag)) {
        if (timeout_ms-- == 0U) {
            return false;
        }
        delay_1ms(1U);
    }
    return true;
}

void bsp_i2c_init(void)
{
    rcu_periph_clock_enable(RCU_GPIOA);
    rcu_periph_clock_enable(I2C_PERIPH_RCU);

    gpio_af_set(I2C_GPIO_PORT, I2C_GPIO_AF, I2C_SCL_PIN | I2C_SDA_PIN);
    gpio_mode_set(I2C_GPIO_PORT, GPIO_MODE_AF, GPIO_PUPD_PULLUP, I2C_SCL_PIN | I2C_SDA_PIN);
    gpio_output_options_set(I2C_GPIO_PORT, GPIO_OTYPE_OD, GPIO_OSPEED_50MHZ, I2C_SCL_PIN | I2C_SDA_PIN);

    i2c_deinit(I2C_PERIPH);
    i2c_clock_config(I2C_PERIPH, I2C_SPEED_HZ, I2C_DTCY_2);
    i2c_mode_addr_config(I2C_PERIPH, I2C_I2CMODE_ENABLE, I2C_ADDFORMAT_7BITS, 0x00U);
    i2c_ack_config(I2C_PERIPH, I2C_ACK_ENABLE);
    i2c_enable(I2C_PERIPH);
}

bool bsp_i2c_read_reg(uint8_t dev_addr_7bit, uint8_t reg, uint8_t *data, uint8_t len)
{
    uint8_t addr_w = (uint8_t)(dev_addr_7bit << 1);
    uint8_t addr_r = addr_w;
    uint8_t i;

    if ((data == NULL) || (len == 0U)) {
        return false;
    }

    if (!i2c_wait_flag(I2C_FLAG_I2CBSY, I2C_TIMEOUT_MS)) {
        return false;
    }

    i2c_start_on_bus(I2C_PERIPH);
    if (!i2c_wait_flag(I2C_FLAG_SBSEND, I2C_TIMEOUT_MS)) {
        return false;
    }

    i2c_master_addressing(I2C_PERIPH, addr_w, I2C_TRANSMITTER);
    if (!i2c_wait_flag(I2C_FLAG_ADDSEND, I2C_TIMEOUT_MS)) {
        i2c_stop_on_bus(I2C_PERIPH);
        return false;
    }
    i2c_flag_clear(I2C_PERIPH, I2C_FLAG_ADDSEND);

    i2c_data_transmit(I2C_PERIPH, reg);
    if (!i2c_wait_flag(I2C_FLAG_BTC, I2C_TIMEOUT_MS)) {
        i2c_stop_on_bus(I2C_PERIPH);
        return false;
    }

    i2c_start_on_bus(I2C_PERIPH);
    if (!i2c_wait_flag(I2C_FLAG_SBSEND, I2C_TIMEOUT_MS)) {
        return false;
    }

    i2c_master_addressing(I2C_PERIPH, addr_r, I2C_RECEIVER);
    if (!i2c_wait_flag(I2C_FLAG_ADDSEND, I2C_TIMEOUT_MS)) {
        i2c_stop_on_bus(I2C_PERIPH);
        return false;
    }
    i2c_flag_clear(I2C_PERIPH, I2C_FLAG_ADDSEND);

    for (i = 0U; i < len; i++) {
        if (i == (len - 1U)) {
            i2c_ack_config(I2C_PERIPH, I2C_ACK_DISABLE);
        }
        if (!i2c_wait_flag(I2C_FLAG_RBNE, I2C_TIMEOUT_MS)) {
            i2c_ack_config(I2C_PERIPH, I2C_ACK_ENABLE);
            i2c_stop_on_bus(I2C_PERIPH);
            return false;
        }
        data[i] = i2c_data_receive(I2C_PERIPH);
    }

    i2c_ack_config(I2C_PERIPH, I2C_ACK_ENABLE);
    i2c_stop_on_bus(I2C_PERIPH);
    return true;
}
```

### 5.10 应用层：读 AS5600 角度

**`as5600.c` 最小接口（放在 `User/servo/`，不在 ISR 里调用）：**

```c
bool as5600_magnet_detected(void)
{
    uint8_t st;

    if (!bsp_i2c_read_reg(AS5600_I2C_ADDR_7BIT, AS5600_REG_STATUS, &st, 1U)) {
        return false;
    }
    return (st & AS5600_STATUS_MD) != 0U;
}

uint16_t as5600_read_angle_raw(void)
{
    uint8_t buf[2];
    uint16_t raw;

    if (!bsp_i2c_read_reg(AS5600_I2C_ADDR_7BIT, AS5600_REG_ANGLE, buf, 2U)) {
        return 0xFFFFU;   /* 读失败哨兵值，应用层需判断 */
    }
    raw = (uint16_t)(((uint16_t)buf[0] << 8) | buf[1]);
    return raw & 0x0FFFU;
}
```

**P1 验收步骤：**

1. `systick_config()` → `bsp_i2c_init()`
2. 主循环读 STATUS：旋转磁铁时 **MD 位应为 1**
3. 读 ANGLE：缓慢转轴，raw 应 **0~4095 连续变化**（跨 0° 会有 4095→0 跳变，属正常）
4. `raw × 360 / 4096` 与机械角度大致一致

**调用约束（与 [§4.11](#411-定时器-update-中断控制环节拍) 一致）：**

- **禁止** 在 TIMER13 / ADC 等实时 ISR 里读 I2C（单次读约 100~300 µs，且会阻塞）
- 位置环在 **1 kHz 控制中断中降频**（如每 10 ms 读一次），或放在主循环

### 5.11 初始化顺序

```text
systick_config()
bsp_i2c_init()          /* 或 as5600_init() 内部调 bsp_i2c_init */
/* 与 ADC、motor 无强依赖，顺序可调 */
motor_init()
time_init()
```

在 `Init.c` 的 `init_all()` 中于 `motor_init()` 前后加入 `bsp_i2c_init()` 即可。

### 5.12 Keil 集成注意

- 库文件 `gd32f1x0_i2c.c` 已在工程中（Library 组）
- 自写 **`bsp_i2c.c`** 加入 **User / bsp** 组（当前工程文件名为 `bap_i2c.c`，建议重命名为 `bsp_i2c.c` 与头文件一致）
- 应用层 `as5600.c` 加入 **User / servo** 组
- 本阶段用 **轮询** 即可，无需配置 `I2C0_EV/ER_IRQHandler`

### 5.13 调试速查

| 现象 | 排查 |
|------|------|
| 读回 0xFF / 全失败 | PA9/PA10 是否 **AF4 + 开漏**；是否 `i2c_enable`；AS5600 是否供电（U8 路径） |
| `I2C_FLAG_AERR` | 地址是否为 **0x36**；SDA/SCL 是否接反；上拉是否存在（R14/R15） |
| 总线一直 BSY | 从机挂死：断电重上；软件 `i2c_stop_on_bus` + 超时退出 |
| MD = 0 | 无磁铁 / 气隙过大 / 磁体未径向充磁 |
| 角度噪声大 | 正常量化 ±1 LSB；检查磁强 ML/MH；应用层 unwrap + 滤波 |
| 配成推挽无波形 | I2C 必须 **GPIO_OTYPE_OD** |
| 误改 PA4 | **禁止** — PA4 是 VIN ADC，不是 DIR |

---

## 6. USART（上位机通信 / 调试输出）

本板通过 **PA2 单线半双工** 与上位机（USB 转串口、VOFA+ 等）通信。GPIO 复用见 [§3.6 / §3.7](#37-本板-gpio-引脚速查)；任务拆解与协议见 [功能实现任务拆解 §C-01](./功能实现任务拆解.md)。

> **与 I2C 的区别：** 半双工单线总线建议 **开漏（OD）+ 上拉**（与 I2C 类似）；全双工调试口可用推挽。  
> **与 I2C 的引脚冲突：** PA9/PA10 已用于 **I2C0（AS5600）**，本板通信口用 **PA2 + USART1**，不要用评估板默认的 PA9/PA10 USART0 引脚。

### 6.1 相关文件

| 文件 | 路径 |
|------|------|
| 头文件（查 API） | [`Library/Include/gd32f1x0_usart.h`](../servo_project/Library/Include/gd32f1x0_usart.h) |
| 源文件（实现） | [`Library/Source/gd32f1x0_usart.c`](../servo_project/Library/Source/gd32f1x0_usart.c) |
| DMA 重映射 | [`Library/Include/gd32f1x0_syscfg.h`](../servo_project/Library/Include/gd32f1x0_syscfg.h)（仅 **USART0** 有 TX/RX DMA remap） |
| BSP（本工程） | `User/bsp/bsp_uart.c/h`（Keil 中暂名 `bap_uart.c`，建议改名） |
| 应用层（待建） | `User/servo/comm_usart.c/h`（C-01 协议）、`data_send.c/h`（JustFloat 波形） |

用户代码 **只 `#include "gd32f1x0.h"`**，不要单独 include `gd32f1x0_usart.h`。

### 6.2 基本参数（本板 C-01）

| 项目 | 本板取值 |
|------|----------|
| 外设实例 | **USART1**（APB1） |
| 通信引脚 | **PA2** = USART1_TX，复用 **AF1** |
| 物理层 | **单线半双工**（`usart_halfduplex_enable`） |
| 波特率 | **115200** |
| 帧格式 | **8N1**（8 数据位、无校验、1 停止位） |
| 连接器 | 板载 **P1 Pin1**（信号）+ GND（走线待原理图最终确认） |
| USART 时钟 | **APB1**（`SystemCoreClock = 72 MHz` 时 APB1 通常 36 MHz，`usart_baudrate_set()` 内部读 `CK_APB1`） |
| GPIO 电气 | 半双工：**开漏 OD + 上拉**（板载或外接 1.5k~4.7k 至 3V3） |

**USART0 vs USART1（易混）：**

| 外设 | 总线 | 评估板常用引脚 | 本板 |
|------|------|----------------|------|
| USART0 | APB2 | PA9 TX / PA10 RX | **与 I2C0 冲突，不用** |
| USART1 | APB1 | 视封装而定 | **PA2 半双工（C-01）** |

> 语雀版任务表写「USART0 + PA2」，与主任务表「USART1 + PA2」不一致；**以 GD32F130 数据手册 AF 表为准**（PA2 → USART1_TX，AF1）。固件统一用 **USART1**。

### 6.3 工作模式速查

| 模式 | API | 引脚 | 本板用途 |
|------|-----|------|----------|
| 全双工 | 默认；`REN`+`TEN` 各接 TX/RX | 2 线 | 不采用（引脚占用） |
| **半双工** | `usart_halfduplex_enable()` | **单线**（接 TX 脚） | **C-01 通信** |
| 单线 + 中断收发 | 半双工 + `RBNE`/`TC` 中断 | PA2 | 命令协议、环形缓冲 |
| DMA 发送 | `usart_dma_transmit_config` | — | **仅 USART0** 有 SYSCFG remap；本板 PA2 用 **中断/轮询 TX** |

半双工模式下，同一根线分时发送/接收：发完一帧后须等 `USART_FLAG_TC`，再切回接收。

### 6.4 常用 API

**初始化：**

| 函数 | 作用 |
|------|------|
| `usart_deinit(usart)` | 复位 USART |
| `usart_baudrate_set(usart, baud)` | 波特率（如 `115200U`） |
| `usart_word_length_set(usart, wlen)` | 8/9 位数据 |
| `usart_stop_bit_set(usart, stblen)` | 停止位 |
| `usart_parity_config(usart, parity)` | 校验（8N1 用 `USART_PM_NONE`） |
| `usart_halfduplex_enable(usart)` | **单线半双工** |
| `usart_transmit_config(usart, USART_TRANSMIT_ENABLE)` | 使能发送 |
| `usart_receive_config(usart, USART_RECEIVE_ENABLE)` | 使能接收 |
| `usart_enable(usart)` | 使能外设 |

**轮询收发：**

| 函数 | 作用 |
|------|------|
| `usart_data_transmit(usart, byte)` | 写发送缓冲 |
| `usart_data_receive(usart)` | 读接收缓冲 |
| `usart_flag_get(usart, flag)` | 查 `TBE`/`TC`/`RBNE` 等 |
| `usart_flag_clear(usart, flag)` | 清标志 |

**中断：**

| 函数 | 作用 |
|------|------|
| `usart_interrupt_enable(usart, USART_INT_RBNE)` | 接收非空中断 |
| `usart_interrupt_enable(usart, USART_INT_TC)` | 发送完成中断 |
| `usart_interrupt_flag_get` / `usart_interrupt_flag_clear` | 读/清中断标志 |
| `nvic_irq_enable(USART1_IRQn, pre, sub)` | 开 NVIC |

**DMA（仅 USART0，本板一般不启用）：**

| 函数 | 作用 |
|------|------|
| `usart_dma_transmit_config(usart, USART_TRANSMIT_DMA_ENABLE)` | TX DMA |
| `usart_dma_receive_config(usart, USART_RECEIVE_DMA_ENABLE)` | RX DMA |
| `syscfg_dma_remap_enable(SYSCFG_DMA_REMAP_USART0TX)` | 默认 CH1，可 remap 到 CH3 |

### 6.5 常用宏

**外设与时钟：**

```c
USART1, RCU_USART1                  /* 本板 C-01 */
USART1_IRQn                         /* 中断号 28 */
GPIO_AF_1                           /* PA2 的 USART1 复用 */
115200U
```

**帧格式（8N1）：**

```c
USART_WL_8BIT
USART_PM_NONE                       /* 无校验；须先 parity 再 word length */
USART_STB_1BIT
USART_TRANSMIT_ENABLE / USART_RECEIVE_ENABLE
```

**轮询常用标志：**

| 宏 | 含义 |
|----|------|
| `USART_FLAG_TBE` | 发送缓冲空，可写下一字节 |
| `USART_FLAG_TC` | 整帧发送完成（半双工切接收前须等） |
| `USART_FLAG_RBNE` | 接收缓冲非空，可读 |
| `USART_FLAG_IDLE` | 空闲帧（常用于帧定界） |
| `USART_FLAG_ORERR` | 溢出，需清 |

**中断常用：**

```c
USART_INT_RBNE      /* 收 1 字节 */
USART_INT_TC        /* 发完 */
USART_INT_IDLE      /* 一包结束 */
USART_INT_ERR       /* 帧/噪声/溢出 */
```

### 6.6 本板 USART 引脚速查

| 引脚 | 信号 | AF | GPIO 模式（半双工） |
|------|------|----|---------------------|
| PA2 | USART1_TX（半双工单线） | AF1 | `GPIO_MODE_AF` + **开漏 OD** + 上拉 |

```c
#define COMM_USART              USART1
#define COMM_USART_RCU          RCU_USART1
#define COMM_USART_IRQn         USART1_IRQn
#define COMM_GPIO_PORT          GPIOA
#define COMM_GPIO_PIN           GPIO_PIN_2
#define COMM_GPIO_AF            GPIO_AF_1
#define COMM_BAUDRATE           115200U
```

### 6.7 半双工初始化 — 标准八步

```text
① 开时钟（GPIOA + USART1）
② PA2：AF1 + 开漏 + 上拉
③ usart_deinit → 波特率 115200 → 8N1
④ usart_halfduplex_enable
⑤ usart_transmit_config(ENABLE) + usart_receive_config(ENABLE)
⑥ （可选）usart_interrupt_enable + nvic_irq_enable
⑦ usart_enable
⑧ 收发：轮询等 TBE/TC/RBNE，或中断 + 环形缓冲
```

```c
void bsp_usart_halfduplex_init(void)
{
    rcu_periph_clock_enable(RCU_GPIOA);
    rcu_periph_clock_enable(RCU_USART1);

    gpio_af_set(GPIOA, GPIO_AF_1, GPIO_PIN_2);
    gpio_mode_set(GPIOA, GPIO_MODE_AF, GPIO_PUPD_PULLUP, GPIO_PIN_2);
    gpio_output_options_set(GPIOA, GPIO_OTYPE_OD, GPIO_OSPEED_50MHZ, GPIO_PIN_2);

    usart_deinit(USART1);
    usart_baudrate_set(USART1, 115200U);
    usart_parity_config(USART1, USART_PM_NONE);
    usart_word_length_set(USART1, USART_WL_8BIT);
    usart_stop_bit_set(USART1, USART_STB_1BIT);
    usart_halfduplex_enable(USART1);
    usart_transmit_config(USART1, USART_TRANSMIT_ENABLE);
    usart_receive_config(USART1, USART_RECEIVE_ENABLE);
    usart_enable(USART1);
}
```

**配置顺序注意：** `usart_parity_config` / `usart_word_length_set` 会短暂 `UEN=0`；须在 `usart_enable()` 前完成。半双工须在 `usart_enable()` 前调用 `usart_halfduplex_enable()`。

### 6.8 轮询发送 / 接收（最小可运行）

**发 1 字节：**

```c
void usart_putc(uint32_t usart, uint8_t ch)
{
    while (RESET == usart_flag_get(usart, USART_FLAG_TBE)) { }
    usart_data_transmit(usart, ch);
    while (RESET == usart_flag_get(usart, USART_FLAG_TC)) { }
    usart_flag_clear(usart, USART_FLAG_TC);
}
```

**收 1 字节（带超时）：**

```c
bool usart_getc_timeout(uint32_t usart, uint8_t *ch, uint32_t timeout_loops)
{
    while (RESET == usart_flag_get(usart, USART_FLAG_RBNE)) {
        if (timeout_loops-- == 0U) {
            return false;
        }
    }
    *ch = (uint8_t)usart_data_receive(usart);
    return true;
}
```

**半双工收发切换：** 发送前无需额外切换；发送完成后 `TC=1` 即可接收。多字节发送时，最后一字节发完后等 `TC`，再读 `RBNE`。

### 6.9 中断 + 环形缓冲（C-01 推荐）

与 [§4.11](#411-定时器-update-中断控制环节拍) 分工：

| 位置 | 允许 | 禁止 |
|------|------|------|
| **USART ISR** | 收字节入 RX ring；TX ring 取字节发 | 长 `printf`、浮点格式化、I2C |
| **TIMER13 ISR** | `motor_set_duty`、置采样标志 | `printf`、I2C、阻塞 UART |
| **main 慢任务** | 解析协议、组帧、`printf`、JustFloat | 长时间关中断 |

**RX 模板（`USART1_IRQHandler`）：**

```c
void USART1_IRQHandler(void)
{
    if (RESET != usart_interrupt_flag_get(USART1, USART_INT_FLAG_RBNE)) {
        uint8_t b = (uint8_t)usart_data_receive(USART1);
        ringbuf_put(&rx_rb, b);   /* 应用层实现 */
    }
    if (RESET != usart_interrupt_flag_get(USART1, USART_INT_FLAG_IDLE)) {
        usart_interrupt_flag_clear(USART1, USART_INT_FLAG_IDLE);
        /* 可选：一帧结束，置 packet_ready */
    }
}
```

初始化时开 `USART_INT_RBNE`；帧定界可用 `USART_INT_IDLE`（须先 `usart_receiver_timeout_enable` 或读一次清 IDLE）。

**NVIC 优先级建议：** USART `pre=2`（低于 TIMER13 的 `pre=1`），避免串口抢占 1 kHz 控制环；高于非实时任务即可。

### 6.10 JustFloat 波形输出（VOFA+ / SerialPlot）

工程内 `data_send.c` 原为 STM32 HAL + DMA 模板，移植到 GD32 时：

1. **本板 PA2 走 USART1**，无 USART1 DMA → 用 **中断 TX** 或 **主循环轮询** 发送。
2. 帧格式不变：N 个 `float`（小端） + 帧尾 `00 00 80 7F`。
3. 在 **主循环**或 **100 Hz 编码器任务**后调用，勿在 TIMER13 ISR 里发浮点波形。

```c
static const uint8_t JF_TAIL[4] = {0x00, 0x00, 0x80, 0x7F};

void justfloat_send_poll(const float *ch, uint8_t ch_num)
{
    uint8_t buf[JF_MAX_CH * 4U + 4U];
    uint16_t n = 0U;
    uint8_t i;

    for (i = 0U; i < ch_num; i++) {
        memcpy(&buf[n], &ch[i], 4U);
        n += 4U;
    }
    memcpy(&buf[n], JF_TAIL, 4U);
    n += 4U;
    for (i = 0U; i < n; i++) {
        usart_putc(USART1, buf[i]);
    }
}
```

**示例通道：** `motor_encoder_degree`、`motor_speed_degree`、`motor_adc_i_bus` 等（见 `servo_config.h` 的 `sensor_data_t`）。

### 6.11 USART0 + DMA（了解即可，本板不默认使用）

GD32F130 的 **USART0** 支持 TX/RX DMA，默认 **TX→DMA CH1、RX→DMA CH2**，可通过 SYSCFG 改映射：

```c
syscfg_dma_remap_enable(SYSCFG_DMA_REMAP_USART0TX);  /* → CH3 */
syscfg_dma_remap_enable(SYSCFG_DMA_REMAP_USART0RX);  /* → CH4 */
```

评估板 USART0 在 **PA9/PA10**，与本板 **I2C0（AS5600）冲突**，故本工程不用此路径。若仅用 USART0 调试、不接 AS5600，可参考下列初始化顺序：

```text
① USART0 GPIO（PA9/PA10 AF1）全双工 115200 8N1
② DMA CH1：内存 → &USART_TDATA(USART0)，normal 模式
③ usart_dma_transmit_config(USART0, USART_TRANSMIT_DMA_ENABLE)
④ DMA FTF 中断里清 busy、允许下一帧
```

**DMA 通道占用注意：** ADC 已占 **DMA CH0**（[§7.12](#712-timer-触发--dma--中断完整链路)），USART0 TX 默认 CH1，互不冲突。

### 6.12 初始化顺序（放入 `init_all`）

```text
systick_config()
gpio_init()
adc_init()              /* DMA CH0 给 ADC */
motor_init()            /* TIMER2 PWM + TRGO */
encoder_init()          /* I2C0 @ PA9/PA10 */
bsp_usart_halfduplex_init()   /* USART1 @ PA2，与 I2C 无冲突 */
time_init()             /* TIMER13 1 kHz */
```

通信初始化放在 `encoder_init()` 之后即可；USART 与 I2C 引脚独立。

### 6.13 BSP 文件约定

| 层次 | 文件 | 职责 |
|------|------|------|
| BSP | `bsp_uart.c/h` | GPIO、波特率、半双工、`putc`/`getc`、IRQ 入口 |
| 应用 | `comm_usart.c/h` | 环形缓冲、帧解析、echo、命令表（C-02） |
| 应用 | `data_send.c/h` | JustFloat 组帧 |

`bsp_uart.c` 当前为空壳，按 §6.7 填充；Keil 工程组内文件名 `bap_uart.c` 建议改为 `bsp_uart.c` 与目录一致。

### 6.14 Keil 集成注意

- 库文件 `gd32f1x0_usart.c` 已在工程中（Library 组）
- 自写 `bsp_uart.c` 需 **Add to Group → bsp**
- 使用 `USART1_IRQHandler` 时，在 `gd32f1x0_it.c` 实现或 `bsp_uart.c` 中实现（二选一，避免重复定义）
- `printf` 重定向：实现 `fputc` 内部调 `usart_putc`，并勾选 **Use MicroLIB** 或半主机关闭

### 6.15 调试速查

| 现象 | 排查 |
|------|------|
| 完全无波形 | PA2 是否 AF1；是否 `usart_enable`；USB 转串口 GND 共地；波特率 115200 |
| 乱码 | 时钟/波特率误差；是否 8N1；半双工是否 enable |
| 只能发不能收 | 半双工是否等 `TC` 再读 `RBNE`；是否 OD+上拉 |
| 与编码器同时异常 | 勿在 TIMER13 ISR 里阻塞 UART；勿与 I2C 同时 `__disable_irq` 过久 |
| `printf` 卡死 | 未实现 `fputc`；`TBE` 死等（USART 未使能） |
| 想 DMA 发 JustFloat | 本板 PA2 无 USART1 DMA；改中断 TX 或放弃 AS5600 用 USART0（不推荐） |
| 评估板例程不工作 | 例程是 USART0@PA9/10，本板须改 **USART1@PA2** |

---

## 7. ADC

ADC 用于读取引脚模拟电压并转换为数字量。GPIO 模拟输入见 [§3.3 / §3.7](#37-本板-gpio-引脚速查)；本板电流/电压换算与标定见 [M-02-M03-感知采集实现笔记 §4](./M-02-M03-感知采集实现笔记.md)。

- P1 轮询采样：[§7.7](#77-单通道轮询采样--标准七步)
- 电流环同步采样（TIMER TRGO + DMA + 中断）：[§7.12](#712-timer-触发--dma--中断完整链路)，TIMER 侧见 [§4.10](#410-中心对齐-pwm--trgo-触发-adc电流环同步)

### 7.1 相关文件

| 文件 | 路径 |
|------|------|
| 头文件（查 API） | [`Library/Include/gd32f1x0_adc.h`](../servo_project/Library/Include/gd32f1x0_adc.h) |
| 源文件（实现） | [`Library/Source/gd32f1x0_adc.c`](../servo_project/Library/Source/gd32f1x0_adc.c) |
| 时钟配置 | [`Library/Include/gd32f1x0_rcu.h`](../servo_project/Library/Include/gd32f1x0_rcu.h) · `rcu_adc_clock_config()` |

与 GPIO 相同，用户代码 **只 `#include "gd32f1x0.h"`**，不要单独 include `gd32f1x0_adc.h`。

### 7.2 基本参数（GD32F130）

| 项目 | 说明 |
|------|------|
| 分辨率 | **12-bit**，右对齐时结果 **0 ~ 4095** |
| 参考电压 | 以 **VDDA** 为准，本板为 **3.3 V** |
| 通道组 | **规则组（Regular）** 与 **注入组（Inserted）**；本工程常用规则组 |
| 引脚模式 | `GPIO_MODE_ANALOG`（见 §3.3），无需 AF 复用 |
| ADC 时钟 | 不得超过 **14 MHz**；常用 `RCU_ADCCK_APB2_DIV6` |

**电压换算（BSP 层只到引脚电压）：**

```text
V_pin = raw × VDDA / 4096
```

物理量（母线 VIN、母线电流等）需结合硬件分压/运放，在应用层 `adc_monitor` 中换算，见 M-02 笔记。

### 7.3 ADC 时钟

ADC 挂在 **APB2**。除 `RCU_ADC` 外还需配置 ADC 专用时钟：

```c
rcu_periph_clock_enable(RCU_ADC);
rcu_adc_clock_config(RCU_ADCCK_APB2_DIV6);   /* 常用：APB2/6，保证 ≤ 14 MHz */
```

| 宏 | 含义 |
|----|------|
| `RCU_ADCCK_APB2_DIV2` | APB2 / 2 |
| `RCU_ADCCK_APB2_DIV4` | APB2 / 4 |
| `RCU_ADCCK_APB2_DIV6` | APB2 / 6（推荐起点） |
| `RCU_ADCCK_APB2_DIV8` | APB2 / 8 |
| `RCU_ADCCK_IRC14M` | 内部 14 MHz（GD32F130/150 可选） |

若 `SystemCoreClock = 72 MHz`、APB2 不分频，则 `APB2/6 = 12 MHz`，在规格范围内。

### 7.4 常用 API

**初始化与校准：**

| 函数 | 作用 |
|------|------|
| `adc_deinit()` | 复位 ADC（初始化前调用） |
| `adc_enable()` | 使能 ADC |
| `adc_disable()` | 关闭 ADC |
| `adc_calibration_enable()` | 启动校准（须在 `adc_enable()` 之后） |

**规则组配置：**

| 函数 | 作用 |
|------|------|
| `adc_data_alignment_config(align)` | 右对齐 / 左对齐 |
| `adc_channel_length_config(group, len)` | 规则组或注入组通道个数 |
| `adc_regular_channel_config(rank, ch, sample_time)` | 规则组第 `rank` 个转换的通道与采样时间 |
| `adc_external_trigger_source_config(group, src)` | 外部触发源（如 TIMER TRGO） |
| `adc_external_trigger_config(group, ENABLE/DISABLE)` | 是否允许外部触发 |
| `adc_software_trigger_enable(group)` | 软件触发一次转换 |
| `adc_regular_data_read()` | 读规则组结果寄存器 |

**标志位：**

| 函数 | 作用 |
|------|------|
| `adc_flag_get(flag)` | 查询 EOC 等标志 |
| `adc_flag_clear(flag)` | 清除标志 |

**扫描 / DMA / 特殊功能（进阶）：**

| 函数 | 作用 |
|------|------|
| `adc_special_function_config(ADC_SCAN_MODE, ENABLE)` | 扫描模式：多通道依次转换 |
| `adc_dma_mode_enable()` | 转换完成自动 DMA 搬运 |
| `adc_discontinuous_mode_config(...)` | 间断模式 |
| `adc_watchdog_*()` | 模拟看门狗阈值 |

### 7.5 常用宏

**通道编号（`adc_regular_channel_config` 第 2 参数）：**

```c
ADC_CHANNEL_0 ~ ADC_CHANNEL_15    /* 对应 ADC_IN0 ~ ADC_IN15 */
ADC_CHANNEL_16                    /* 温度传感器（需 adc_tempsensor_vrefint_enable） */
ADC_CHANNEL_17                    /* 内部 Vrefint */
```

**本板常用：**

| 引脚 | ADC 输入 | 通道宏 |
|------|----------|--------|
| PA3 | ADC_IN3 | `ADC_CHANNEL_3` |
| PA4 | ADC_IN4 | `ADC_CHANNEL_4` |

**采样时间：**

| 宏 | 采样周期 |
|----|----------|
| `ADC_SAMPLETIME_1POINT5` | 1.5 |
| `ADC_SAMPLETIME_7POINT5` | 7.5 |
| `ADC_SAMPLETIME_28POINT5` | 28.5 |
| `ADC_SAMPLETIME_55POINT5` | 55.5（**本板推荐起点**，前端有 RC 滤波） |
| `ADC_SAMPLETIME_239POINT5` | 239.5（高阻抗源） |

**数据对齐：**

| 宏 | 含义 |
|----|------|
| `ADC_DATAALIGN_RIGHT` | 右对齐，12-bit 占低 12 位（常用） |
| `ADC_DATAALIGN_LEFT` | 左对齐 |

**通道组与触发：**

| 宏 | 含义 |
|----|------|
| `ADC_REGULAR_CHANNEL` | 规则组 |
| `ADC_INSERTED_CHANNEL` | 注入组 |
| `ADC_EXTTRIG_REGULAR_NONE` | 规则组仅软件触发 |
| `ADC_EXTTRIG_REGULAR_T2_TRGO` | TIMER2 TRGO 触发（与 PWM 同步采样时用） |
| `ADC_FLAG_EOC` | 规则组转换结束 |
| `ADC_FLAG_STRC` | 规则组转换开始 |

### 7.6 引脚与通道对应（GD32F130）

PA 口与 ADC 通道一一对应：**PAx → ADC_INx → ADC_CHANNEL_x**。  
配置 GPIO 时用 `GPIO_MODE_ANALOG`，**不要**设为 AF 或数字输入。

本板（16999-PS26040802）：

| 引脚 | 信号 | 硬件来源 |
|------|------|----------|
| PA3 | 母线电流采样 | GS8591 运放输出 → R11/C15 滤波 |
| PA4 | 母线电压采样 | VIN 分压 + 钳位 |

> **注意：** PA4 在本板是 **VIN ADC**，不是 AS5600 DIR（DIR 已在硬件接地）。

### 7.7 单通道轮询采样 — 标准七步

与 GPIO「开时钟 → 设模式」对应，最小可运行流程：

```text
① 开时钟（GPIOA + ADC），配置 ADC 时钟分频
② PA 口设为 GPIO_MODE_ANALOG
③ adc_deinit → 右对齐 → 规则组长度 1 → 软件触发
④ adc_enable → 短延时 → adc_calibration_enable
⑤ 每次采样：adc_regular_channel_config(rank, channel, sample_time)
⑥ adc_software_trigger_enable → 等待 ADC_FLAG_EOC → 清标志
⑦ adc_regular_data_read() 得 raw
```

```c
rcu_periph_clock_enable(RCU_GPIOA);
rcu_periph_clock_enable(RCU_ADC);
rcu_adc_clock_config(RCU_ADCCK_APB2_DIV6);                        /* ① */

gpio_mode_set(GPIOA, GPIO_MODE_ANALOG, GPIO_PUPD_NONE,
              GPIO_PIN_3 | GPIO_PIN_4);                          /* ② */

adc_deinit();
adc_data_alignment_config(ADC_DATAALIGN_RIGHT);
adc_channel_length_config(ADC_REGULAR_CHANNEL, 1U);
adc_external_trigger_source_config(ADC_REGULAR_CHANNEL, ADC_EXTTRIG_REGULAR_NONE);
adc_external_trigger_config(ADC_REGULAR_CHANNEL, DISABLE);
adc_enable();                                                     /* ④ */

for (volatile uint32_t i = 0U; i < 10000U; i++) { }             /* enable 后需稳定一段时间 */
adc_calibration_enable();

/* ⑥⑦ 读 CH3 示例 */
adc_regular_channel_config(0U, ADC_CHANNEL_3, ADC_SAMPLETIME_55POINT5);
adc_software_trigger_enable(ADC_REGULAR_CHANNEL);
while (RESET == adc_flag_get(ADC_FLAG_EOC)) { }
adc_flag_clear(ADC_FLAG_EOC);
uint16_t raw = adc_regular_data_read();
```

多通道时 **每次改通道** 都要重新 `adc_regular_channel_config` 再触发；或改用 §7.12 TIMER 触发 + DMA。

### 7.8 两种采样模式对比

| 模式 | 触发 | 搬运 | 适用阶段 |
|------|------|------|----------|
| **§7.7 轮询** | 软件 | CPU 读 DR | P1 标定、调试 |
| **§7.12 同步** | TIMER2 TRGO | DMA + 中断 | P4 电流环、过流保护 |

---

### 7.12 TIMER 触发 + DMA + 中断（完整链路）

与 [§4.10](#410-中心对齐-pwm--trgo-触发-adc电流环同步) 配合使用：**TIMER2 持续输出 PWM 并发出 TRGO → ADC 扫描采样 → DMA 写入内存 → 中断里处理**。

#### 7.12.1 整体时序

```text
  TIMER2 PWM 周期
  ────┐     ┌────┐     ┌────
      └─────┘    └─────┘
            ↑ TRGO（UPDATE）
            │
      ADC 扫描 CH3 → CH4
            │
      DMA 写入 buf[0], buf[1]
            │
      ADC EOC 中断（或 DMA FTF）
            │
      电流环 PI → 更新 CCR（影子，下周期生效）
```

**本板一次触发得到：** `buf[0]` = PA3 电流 raw，`buf[1]` = PA4 电压 raw。

#### 7.12.2 涉及外设与文件

| 外设 | 库文件 | 作用 |
|------|--------|------|
| TIMER2 | `gd32f1x0_timer.c` | 中心对齐 PWM + TRGO |
| ADC | `gd32f1x0_adc.c` | 扫描、外部触发、DMA 请求 |
| DMA | `gd32f1x0_dma.c` | 搬运 `ADC_RDATA` → 内存 |
| NVIC | `gd32f1x0_misc.c` | 开 `ADC_CMP_IRQn` 或 `DMA_Channel0_IRQn` |
| SYSCFG | `gd32f1x0_syscfg.c` | 可选：ADC DMA 映射到 CH1 |

#### 7.12.3 初始化顺序

```text
① bsp_adc GPIO + ADC 时钟/校准（§7.7 前半）
② ADC：扫描 2 通道、外部触发 T2_TRGO、使能 DMA
③ DMA：CH0 循环模式，外设 = ADC_RDATA，内存 = adc_buf[]
④ NVIC：使能 ADC EOC 中断（或 DMA FTF）
⑤ TIMER2：中心对齐 PWM + TRGO + CCR 影子（§4.10.5）
⑥ timer_enable → 自动开始周期性采样
```

**顺序要点：** ADC/DMA/NVIC 就绪后再 `timer_enable()`，避免首次 TRGO 丢失。

#### 7.12.4 ADC 侧配置

```c
#define ADC_DMA_BUF_LEN    2U    /* 扫描 2 通道：CH3 + CH4 */
static uint16_t adc_dma_buf[ADC_DMA_BUF_LEN];

/* §7.7 已完成：GPIO 模拟、时钟、校准、adc_enable */

adc_special_function_config(ADC_SCAN_MODE, ENABLE);
adc_channel_length_config(ADC_REGULAR_CHANNEL, 2U);
adc_regular_channel_config(0U, ADC_CHANNEL_3, ADC_SAMPLETIME_55POINT5);
adc_regular_channel_config(1U, ADC_CHANNEL_4, ADC_SAMPLETIME_55POINT5);

adc_external_trigger_source_config(ADC_REGULAR_CHANNEL, ADC_EXTTRIG_REGULAR_T2_TRGO);
adc_external_trigger_config(ADC_REGULAR_CHANNEL, ENABLE);

adc_dma_mode_enable();
adc_interrupt_enable(ADC_INT_EOC);
```

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 扫描模式 | ENABLE | 一次触发依次采 CH3、CH4 |
| 规则组长度 | 2 | rank0=CH3，rank1=CH4 |
| 触发源 | `ADC_EXTTRIG_REGULAR_T2_TRGO` | 接 TIMER2 TRGO |
| DMA | ENABLE | 每完成一次转换自动搬运 |

#### 7.12.5 DMA 侧配置

GD32F130 默认 **ADC DMA 请求 → DMA CH0**。若 CH0 已被占用，可 `syscfg_dma_remap_enable(SYSCFG_DMA_REMAP_ADC)` 改映射到 **CH1**（见 `gd32f1x0_syscfg.h`）。

```c
dma_parameter_struct dma_init_struct;

rcu_periph_clock_enable(RCU_DMA);
dma_deinit(DMA_CH0);
dma_struct_para_init(&dma_init_struct);

dma_init_struct.periph_addr  = (uint32_t)&ADC_RDATA;
dma_init_struct.periph_width = DMA_PERIPHERAL_WIDTH_16BIT;
dma_init_struct.periph_inc   = DMA_PERIPH_INCREASE_DISABLE;
dma_init_struct.memory_addr  = (uint32_t)adc_dma_buf;
dma_init_struct.memory_width = DMA_MEMORY_WIDTH_16BIT;
dma_init_struct.memory_inc   = DMA_MEMORY_INCREASE_ENABLE;
dma_init_struct.number       = ADC_DMA_BUF_LEN;
dma_init_struct.direction    = DMA_PERIPHERAL_TO_MEMORY;
dma_init_struct.priority     = DMA_PRIORITY_HIGH;

dma_init(DMA_CH0, &dma_init_struct);
dma_circulation_enable(DMA_CH0);
dma_channel_enable(DMA_CH0);
```

| 参数 | 说明 |
|------|------|
| 外设地址 | `&ADC_RDATA`，固定不变 |
| 宽度 | 16-bit（与 ADC 右对齐 12-bit 一致） |
| 循环模式 | ENABLE，持续覆盖 `adc_dma_buf` |
| 传输数量 | 2（与扫描通道数相同） |

> **双缓冲进阶：** 缓冲长度设为 4（两组 I/V），开 DMA **HTF + FTF** 中断，一组处理时另一组采样。

#### 7.12.6 中断配置

**方案 A — ADC EOC 中断（推荐起步）**

扫描 2 通道完成后产生一次 EOC，与 PWM 周期一一对应，适合电流环。

```c
nvic_irq_enable(ADC_CMP_IRQn, 0U, 0U);   /* GD32F130：ADC 与 CMP 共用中断 */
```

**方案 B — DMA FTF 中断**

适合更长缓冲、双缓冲；需在 `gd32f1x0_it.c` 写 `DMA_Channel0_IRQHandler`。

```c
dma_interrupt_enable(DMA_CH0, DMA_INT_FTF);
nvic_irq_enable(DMA_Channel0_IRQn, 0U, 0U);
```

#### 7.12.7 中断服务函数（模板）

`gd32f1x0_it.c` 中实现。**只做电流环 + 写 CCR**；速度环降频；位置环与 I2C 在慢任务。

```c
void ADC_CMP_IRQHandler(void)
{
    if (RESET != adc_interrupt_flag_get(ADC_INT_FLAG_EOC)) {
        adc_interrupt_flag_clear(ADC_INT_FLAG_EOC);

        uint16_t i_raw = adc_dma_buf[0];   /* CH3 电流 */
        uint16_t v_raw = adc_dma_buf[1];   /* CH4 电压 */

        /* 1. raw → i_bus / v_bus（标定公式见 M-02 笔记） */
        /* 2. 电流环 PI：i_target → duty */
        /* 3. motor_set_duty(duty) 或写 TIMER2 CCR（§4.10 影子） */
        /* 4. 可选：过流/欠压判断 → motor_disable() */
    }
}
```

**ISR 约束：**

- 保持 **短**（目标 < 50 µs @ 10 kHz 控制周期）
- **禁止** I2C 读 AS5600
- 速度/位置环每 N 次中断执行一次，或放 SysTick

#### 7.12.8 `board_config.h` 建议宏

```c
#define ADC_PWM_TIMER             TIMER2
#define ADC_SAMPLE_RATE_HZ        20000U
#define ADC_PWM_PERIOD_CA         1799U   /* 中心对齐 20 kHz @ 72 MHz */
#define ADC_DMA_CHANNEL           DMA_CH0
#define ADC_DMA_BUF_LEN           2U
```

#### 7.12.9 调试速查

| 现象 | 排查 |
|------|------|
| `adc_dma_buf` 不更新 | TIMER2 是否 enable；TRGO 是否 UPDATE；ADC 外部触发是否 ENABLE |
| 只有 buf[0] 变 | 扫描长度是否为 2；rank0/rank1 通道是否正确 |
| 中断不进 | `ADC_CMP_IRQn` 是否使能；是否 `adc_interrupt_enable(ADC_INT_EOC)` |
| 采样率 2 倍于预期 | 中心对齐 UPDATE 频率；示波器测 TRGO |
| DMA 无请求 | `adc_dma_mode_enable()`；CH0 是否被占用；查 SYSCFG remap |
| 控制振荡 | 电流环 Kp/Ki 过大；CCR 未开影子；采样不在谷底 |

应用层换算、滤波、三环分频见 [M-02-M03 感知采集实现笔记 §4](./M-02-M03-感知采集实现笔记.md) 及后续 M-04/M-05 任务。

---

### 7.9 BSP 示例：`bsp_adc.c/h`

**`board_config.h`（引脚宏）：**

```c
#define ADC_CURRENT_PORT          GPIOA
#define ADC_CURRENT_PIN           GPIO_PIN_3
#define ADC_CURRENT_CHANNEL       ADC_CHANNEL_3

#define ADC_VBUS_PORT             GPIOA
#define ADC_VBUS_PIN              GPIO_PIN_4
#define ADC_VBUS_CHANNEL          ADC_CHANNEL_4
```

**`bsp_adc.h`：**

```c
#ifndef BSP_ADC_H
#define BSP_ADC_H
#include "gd32f1x0.h"

void bsp_adc_init(void);
uint16_t bsp_adc_read_channel(uint8_t channel);

#endif
```

**`bsp_adc.c`（核心逻辑）：**

```c
#include "bsp_adc.h"
#include "board_config.h"

void bsp_adc_init(void)
{
    rcu_periph_clock_enable(RCU_GPIOA);
    rcu_periph_clock_enable(RCU_ADC);
    rcu_adc_clock_config(RCU_ADCCK_APB2_DIV6);

    gpio_mode_set(ADC_CURRENT_PORT, GPIO_MODE_ANALOG, GPIO_PUPD_NONE, ADC_CURRENT_PIN);
    gpio_mode_set(ADC_VBUS_PORT, GPIO_MODE_ANALOG, GPIO_PUPD_NONE, ADC_VBUS_PIN);

    adc_deinit();
    adc_data_alignment_config(ADC_DATAALIGN_RIGHT);
    adc_channel_length_config(ADC_REGULAR_CHANNEL, 1U);
    adc_external_trigger_source_config(ADC_REGULAR_CHANNEL, ADC_EXTTRIG_REGULAR_NONE);
    adc_external_trigger_config(ADC_REGULAR_CHANNEL, DISABLE);
    adc_enable();

    for (volatile uint32_t i = 0U; i < 10000U; i++) { }
    adc_calibration_enable();
}

uint16_t bsp_adc_read_channel(uint8_t channel)
{
    adc_regular_channel_config(0U, channel, ADC_SAMPLETIME_55POINT5);
    adc_software_trigger_enable(ADC_REGULAR_CHANNEL);

    while (RESET == adc_flag_get(ADC_FLAG_EOC)) { }
    adc_flag_clear(ADC_FLAG_EOC);

    return adc_regular_data_read();
}
```

**`main.c` 最小调用（读 raw 验证）：**

```c
#include "bsp_adc.h"
#include "board_config.h"
#include "systick.h"

int main(void)
{
    uint16_t i_raw, v_raw;

    systick_config();
    bsp_adc_init();

    while (1) {
        i_raw = bsp_adc_read_channel(ADC_CURRENT_CHANNEL);
        v_raw = bsp_adc_read_channel(ADC_VBUS_CHANNEL);
        delay_1ms(10);
    }
}
```

`raw → i_bus / v_bus` 的标定系数、滑动平均滤波、过流保护属于应用层 `adc_monitor`，不在 BSP 内实现。

### 7.10 Keil 集成注意

- 库文件 `gd32f1x0_adc.c` 已在工程中（Library 组）
- 自写 `bsp_adc.c` 需手动 **Add to Group → bsp**
- `board_config.h` 只参与编译包含，不必单独勾选编译

### 7.11 调试速查

| 现象 | 排查 |
|------|------|
| raw 全 0 或全 4095 | 引脚是否 `GPIO_MODE_ANALOG`；VDDA 是否 3.3 V；是否执行校准 |
| raw 不随输入变化 | 通道号是否与引脚一致（PA3=CH3）；是否等待 EOC |
| 读数跳动大 | 加长采样时间；应用层加滑动平均；检查前端 RC 滤波 |
| 校准后仍偏差大 | 正常，需应用层按硬件分压/运放标定 |
| 使能后立即校准失败 | `adc_enable()` 与 `adc_calibration_enable()` 之间加短延时 |
| PA4 配成 GPIO 输出 | 本板 PA4 必须保持模拟输入，不能当数字 IO |

---

## 附录 · 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-28 | 初版：从 M-01 笔记拆出 GPIO/RCU 通用内容 |
| 2026-06-28 | §4 补充 Timer/PWM：频率公式、API、六步流程、bsp_pwm 示例 |
| 2026-06-28 | §5/§7 增加 M-02/M-03 感知采集笔记链接 |
| 2026-06-28 | §7 补充 ADC：时钟、API、七步流程、bsp_adc 示例、调试速查 |
| 2026-06-28 | §4.10 / §7.12：中心对齐 PWM、TIMER TRGO 触发 ADC、DMA、中断完整链路 |
| 2026-06-28 | §4.11：TIMER13 UPDATE 中断、控制环节拍（与 TIMER2 PWM 分离） |
| 2026-06-29 | §5：I2C0 主机配置、轮询读寄存器、bsp_i2c 示例、AS5600 STATUS/ANGLE 读法 |
| 2026-06-29 | §6：USART1 半双工 PA2、8N1 115200、轮询/中断/DMA 说明、JustFloat、与 I2C 引脚分工 |
